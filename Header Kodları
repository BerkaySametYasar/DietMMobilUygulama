import 'package:flutter/material.dart';

class Header extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Padding(padding: EdgeInsets.all(50),
    child: Column(
      crossAxisAlignment: CrossAxisAlignment.start,

      children: <Widget>[

        Center(

            child: Text("DietM",style:  TextStyle(color: Colors.green, fontSize: 40),),
        )
        ,
        SizedBox(height: 80,),
        Center(
          child: Text("Oturum Aç", style: TextStyle(color: Colors.green,fontSize: 18),),
        )
      ],
    ),
    );
  }
  }
